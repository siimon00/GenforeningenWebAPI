(function () {
    'use strict';

    module.exports = {
        // Name space for testing CRUD operations
        events: require('./events.json'),
        createEvent: require('./create-event.json')
    };

})();